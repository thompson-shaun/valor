{
  "name": "valor",
  "private": true,
  "scripts": {
    "generate:tracker": "node scripts/generate-tracker.js && node scripts/generate-tracker-pdf.js",
    "generate:xlsx": "node scripts/generate-tracker.js",
    "generate:pdf": "node scripts/generate-tracker-pdf.js",
    "generate:workflow-inputs": "node scripts/generate-workflow-inputs.js",
    "test": "bash tests/vault_test.sh"
  },
  "devDependencies": {
    "exceljs": "^4.4.0",
    "pdfkit": "^0.17.2"
  }
}
